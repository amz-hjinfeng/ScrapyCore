version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"
  #secrets-manager:
     # key: secret-id:json-key:version-stage:version-id
     # key: secret-id:json-key:version-stage:version-id
  #exported-variables:
     # - variable
     # - variable
  #git-credential-helper: yes

phases:
    install:
      runtime-versions:
        dotnet: 2.2
        nodejs: 10
        docker: 18
    build:
      commands:
        - dotnet clean
        - dotnet restore
        - dotnet publish -c Release
    post_build:
      commands:
        - zip -r hydralisk-$CODEBUILD_START_TIME.zip ./ScrapyCore.Hydralisk/bin/Release/netcoreapp2.1/publish/*
        - zip -r kerrigan-$CODEBUILD_START_TIME.zip ./ScrapyCore.Kerrigan/bin/Release/netcoreapp2.1/publish/*
        - zip -r heartofswarmweb-$CODEBUILD_START_TIME.zip ./ScrapyCore.HeartOfSwarm/bin/Release/netcoreapp2.1/publish/*
        - zip -r utralisks-$CODEBUILD_START_TIME.zip ./ScrapyCore.Utralisks/bin/Release/netcoreapp2.1/publish/*
artifacts:
    files:
        - heartofswarmweb-$CODEBUILD_START_TIME.zip
    name: heartofswarmweb
    secondary-artifacts:
      hydralisk:
        files:
          - hydralisk-$CODEBUILD_START_TIME.zip
        name: hydralisk
      kerrigan:
        files:
          - kerrigan-$CODEBUILD_START_TIME.zip
        name: kerrigan
      utralisks:
        files:
          - utralisks-$CODEBUILD_START_TIME.zip
        name: utralisks

  #name: $(date +%Y-%m-%d)
  #discard-paths: yes
  #base-directory: location
#cache:
  #paths:
    # - paths


    